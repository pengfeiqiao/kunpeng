# 工作台重构实施报告

## 原有能力保留补充（2026-09-05 晚间，最新）

本轮在原模块上接回功能，没有另造画布、剪辑或 Agent 执行器。完整重构目标仍不能宣告完成；下文历史缺口不能用本轮构建成功一笔勾销。

| 项目 | 当前状态与可观察入口 | 证据 |
| --- | --- | --- |
| 剧本导入、风格及拆分编辑 | 界面已接入：工坊左侧“剧本与拆解”→“剧本来源 / 拆解结果”；复用 StepScript、StepBreakdown、StyleSelector 和原 buildBreakdownPrompt | scriptTools.test.ts：来源、项目、会话、异步 CAS；不调用真实模型 |
| 风格库与提示词版本 | 界面已接入：提示词工具栏“风格库”，MJ 引擎用原 MJ 库与案例；经典/新版优化入口保留，模板选择随草稿保存 | optimize.test.ts；workspace-composer-smoke 1440/1280/1024/390；无真实改写请求 |
| 角色音色 | 界面已接入：角色→“角色与音色”；原 AssetCard/AiVoiceButton 的描述、生成、上传、试听、移除 | productionSafety、productionPorts、WorkspaceProductionTools 测试；真实付费未验证 |
| 镜头配音及配色 | 界面已接入：镜头→“配音与配色”；复用 AudioPromptsSection/PaletteMenu，保留跟随全片、本镜关闭、本镜覆盖 | 显式 voiceCharacterIds、冻结参考字节、唯一产物路径、裁剪 AudioContext 释放均有 fake ports 测试 |
| 专业画布和剪辑 | 界面仍复用 CanvasView embedded / EditorView embedded；原画布上下文生成器已抽成共享 canvasAgentPrompt | WorkspaceCanvasSurface、canvasAgentPrompt、workspaceCanvasAgent 专项；不声称全部专业工具逐个真机验收 |
| Harness、事实锁与声音助手目标 | 复用原执行器；工作面 envelope 分离原请求与只读上下文，继承原 scope；声音请求排队后校验稳定对象、sourceId、镜号、修订 | workspaceMessage/workspaceToolScope、productionSafety、真实 WorkspaceAssistant 发送回调测试：失效目标发送次数为 0 |

### 安全与回归

- 配音逐项提交，取消不提交；部分成功保留；started/uncertain 记录禁止批量或“重新生成”绕过；仅已完成项目可明确确认后单项重生成。状态日志乱序以完成回执为准，损坏日志按对象暂停生成，不影响编辑和试听。
- 切项目、切对象、卸载或人工修改后，迟到音频只保留在原对象产物记录，不覆盖当前编辑。旧语音渠道与客户端未替换；未动 billingSafety、DSH 上游和 Rust 源码。
- 最终 `npx tsc --noEmit` 通过；`test:harness` **568/568**；`test:wan3` **21/21**；`test:midjourney` **9/9**；`test:dsh-runtime` **5/5**。DSH 的模拟握手超时日志是预期反例，非真机失败。
- `npm run build` 由 Tauri beforeBuildCommand 实际执行并通过。原生 `--debug --bundles app` 编译通过，包含 13 条 Rust 警告及 Vite 的静态/动态混用分包提示；没有把警告描述成零风险。
- 离线 UI：media smoke 验证工具入口不触发生成、草稿与异步结果隔离、取消确认零调用、候选采用；composer smoke 验证四档尺寸；assistant smoke 验证冻结队列、模型范围、真实回执卡及 Skill 展示。所有 provider 端口为 mock，外部请求被拦截。

### 测试变更说明

1. 新增并注册 scriptTools、productionSafety、productionPorts、WorkspaceProductionTools，以及画布上下文/envelope/scope 专项，保护新入口，不删既有断言。
2. optimize.test.ts 扩展原风格改写、取消、空输入、MJ 引擎匹配与草稿不被原地修改的检查。
3. assistantSmoke 的上下文断言改为解析结构化 envelope，不再对已弃用的文本 JSON 前缀做子串匹配；目标、会话、发送数及输入保留检查仍在。
4. composer smoke 仅等待视口内图片完成加载，避免把浏览器对屏外 lazy 图片的正常延迟判成失败；未削弱可见素材与布局检查。
5. media smoke 增加“配音与配色”打开目标断言和零生成调用断言。

### 原生测试版边界

产物：`src-tauri/target/debug/bundle/macos/鲲鹏测试版.app`。这是实际 Tauri 原生 App，不是网页版。测试应用标识 `ai.kunpeng.workspace-test`，不覆盖 `/Applications/鲲鹏.app`，不打 DMG、不提交 Git。

按用户最新要求，交付测试 App 改为正常 HOME 和原有 Tauri 文件访问配置，读取本机已有工程和配置，不再安装隔离启动包装。测试 App 与正式版共用工程文件，用户在测试版保存的修改也会被正式版读取；不同应用标识不代表工程数据隔离。没有读取或记录真实密钥，没有主动提交真实付费任务。

正常数据路径的 debug App 已重新编译成功，构建日志 `/tmp/kunpeng-native-workspace-local-build.log`；主程序经 `file` 核验为 arm64 Mach-O，而非隔离 shell 包装。已通过系统应用入口打开，随后 UI 工具报告用户正在操作，因此未继续切页或读取配置界面；不据此宣称所有旧项目已逐一验证。

`scripts/launch-workspace-native-test.mjs` 仅保留为早先离线验证的隔离工具，不用于本次本机数据测试版启动。此前临时 HOME、网络限制与合成项目只属于历史离线验证；生产 tauri.conf.json 未改。原生界面完整回归仍不能用编译成功代替。

测试图片复用仓库已有风格案例，用来检查显示比例和素材操作，不冒充本剧本已生成的画面。未进行真实模型、付费生成或原用户项目迁移冒烟；声音工具当前保守地总在付费前确认，直接执行偏好在这条新增声音入口上尚未统一。

## 本轮复核摘要（2026-09-05，优先于下方历史记录）

**不能认定完整目标全部完成。** 本轮补齐了多条生产接线及离线界面验证，但专业自由画布全链路与 Agent 硬权限边界仍有缺口。下面“历史记录”保留各小步事实，不代表最新待办；本摘要为当前状态。

### 已接入生产界面

- 顶部统一为 **工坊 / 画布 / 剪辑 / 文档**，56px 单层导航；移除工坊内重复视图工具栏。画布与工坊共用选中对象、媒体和 Inspector，助手保持挂载。文件：ProjectWorkspaceLayout、ProjectWorkspace、workspace.css。
- 剪辑在项目壳内挂载 `EditorView embedded`，保留专业工具；根据已加载项目身份打开，片段“素材来源”按历史文件及版本验证后打开同一 Inspector，不把当前采用版冒充原版本。文件：WorkspaceEditorSurface、WorkspaceTimelineSource、timelineSource、useWorkspaceTimelineSource、EditorEmbeddedTools。
- 文档工作面接真实文档 hook/端口：原剧本文件、结构化规格、创作规则、项目 Skill；规格保存仍走同一命令。文档草稿按项目及来源隔离，异步结果不写另一文件。文件及端口离线验证，不操作真实用户文件。详见 `阶段3文档与剪辑来源集成.md`。
- 助手使用真实 AgentDrawer：明确对象线程，浏览切换不会改变正在编辑的对象；“改这条”才切换目标，普通添加引用不切目标。队列可编辑、取消，卸载再挂载保留；已发出而终态不明的消息不自动重放。真实工具回执驱动媒体卡/提示词改动摘要，真实执行链驱动 Skill 展示；长执行记录折叠。
- 音频媒体进项目内容列表，可独立预览、添加引用；版本采用校验原 owner。未归类素材归类同时投影匹配的专业节点 owner 元数据，不改本次参考、采用状态和已提交任务快照。
- 对象菜单支持添加到对话、从当前视图移除、从项目删除（含影响范围和快照）；工坊移除项可从项目菜单找回，画布隐藏项有找回入口。媒体磁盘文件不删除。
- 列表按可见窗口挂载媒体子树；对象画布保留 ReactFlow 可见节点渲染。100镜/500媒体离线样本中当时挂载10个列表条目，选第100镜的浏览器控制往返约34ms。此数值是单次本地样本，不是跨设备性能保证，也不能替代真实 LLM 流式与系统内存压力测试。

### 数据写入收编

- 旧镜头生成、旧场景多角度生成改为可见草稿、FIFO确认、严格持久化、同一 canvasGen、候选回流；批次结果有明确失败信息，不重放不明提交。
- 旧参考编辑、Agent改单镜提示词/撤销调用同一参考/草稿命令。全量拆镜 replace 经稳定ID匹配、删除快照及任务保护；旧向画布推送是显式投影命令，不再一边复制一边自动采用。
- 专业画布归类通过 `assignProfessionalCanvasMedia` 一次计算业务归属与节点投影。
- **仅有数据接口，未全局启用**：`professionalCanvasEdits` 的显式编辑命令及冲突检查。审查发现，把它直接挂到通用 `canvasStore.updateNode` 会误拦绑定元数据回执和 MG/自定义参数，故未保留全局拦截。专业节点参数编辑、参考连线、旧专业生成的整体收编仍未完成；不能声称双写已全部消除。

### 第18节验收最新自评

| # | 当前结论与可观察证据 |
| --- | --- |
| 1 | 已接项目壳：工坊/画布/剪辑/文档共用56px顶栏；离线真实容器截图。空项目兼容录入保留。 |
| 2 | 已离线验证：镜头→提示词，两次点击到全文；workspace-project-smoke。 |
| 3 | 部分：工坊/对象画布/助手产物/剪辑来源指向同一 Inspector；来源事件与历史版本测试通过。旧专业节点仍有旧入口。 |
| 4 | 已验证工作台/助手/文档1440、1280、1024、390布局；专业剪辑窄屏尚未完成。 |
| 5 | 实际助手组件验证隐藏不占列、新回复不撑开。 |
| 6 | 对象选择、媒体版本预览、采用分开；实际容器候选回流及版本点击测试。 |
| 7 | 部分：工坊写词/参考/replace已收编并保留叙事保护；专业节点及三层Agent硬工具权限还未完整实施。 |
| 8 | 真实组件离线A/B异步保存、迟到冲突拒绝通过。 |
| 9 | 新草稿、旧资产/镜头/场景多角度结果默认候选；不自动进入参考/时间线。旧专业所有生成与恢复路径未逐一UI验证。 |
| 10 | 本次参考、原图路径、编号、冻结历史参数有单测与界面验证；专业节点参考编辑收口未完成。 |
| 11 | 空间与调度单行展开、场景继承/本镜覆盖、更新提醒已验证；不自动重生成视频。 |
| 12 | 历史故事板迁移幂等测试通过；未打开用户真实历史项目。 |
| 13 | 部分：新默认工作面无传入/拉取；兼容推拉转命令，但专业旧路径未全部退役。 |
| 14 | 真实助手队列/线程与跨工作面挂载测试通过；队列保留在进程内，不承诺跨重启恢复。 |
| 15 | 付费纪律专项和mock取消零提交/单次执行通过；真实付费留用户。 |
| 16 | 部分：100镜/500媒体窗口化与选中响应通过；实际LLM流式拖拽、低内存/大量视频回归未完成。 |
| 17 | 专业剪辑内嵌且工具保留，兼容工坊可找；自由画布完整定位与退役清单未完成。 |
| 18 | 未通过最终用户验收：A/B已有离线完整点击链，C分组件生产hook验证；没有把本地样本当作用户认可或付费实测。 |

### 本轮测试与证据

- 最终 `npx tsc --noEmit` 通过；`test:harness` **495/495**、无跳过；包含新增专业画布14项显式命令/归属测试。`npm run build` 通过，4005模块。仍有既有大包、静态/动态导入混用及Browserslist旧数据警告，不据此声称性能问题全部解决。
- `test:wan3` 21/21、`test:midjourney` 9/9、`test:dsh-runtime` 5/5。DSH握手超时负例出现预期stderr，不是测试失败。
- `workspace-project-smoke.mjs` 通过：真实容器、草稿、参数、确认/候选、约束卡、对象画布、归类、100镜窗口化。助手与文件/执行器端口在此脚本普通测试模式中为替身。
- `workspace-assistant-smoke.mjs` 通过：真实助手、队列、媒体/Skill展示、对象事件、直接音频目标、四档宽度；LLM与磁盘端口为替身。
- `workspace-documents-source-smoke.mjs` 通过：真实文档/来源hooks与适配器，原文件读写、A/B草稿、规格保存、Skill导入导出、历史片段来源；仅内存文件系统测试。
- 证据：`workspace-evidence/phase4-100-shots-visible-window.png`、`phase3-documents-source-1280.png`、`phase3-documents-source-390.png`、`final-offline-preview.png`；最新助手截图在 `src/components/workspace/assistant-evidence/assistant-1440.png` 等四档宽度文件。
- `--serve` 提供真实 ProjectWorkspace + 实际助手的隔离浏览器预览；LLM和生成均为本地替身，文档/剪辑等外围端口部分占位，**不等于完整桌面App验收**。CSP禁止外部连接，不读取用户配置，不产生费用。
- 本轮测试版地址：`http://127.0.0.1:53349`，独立预览进程PID 68420；当前登录会话有效。浏览器已验证发送离线消息、打开完整提示词、真实确认队列弹窗，`pageerror=[]`。日志在 `/tmp/kunpeng-workspace-preview-20260905.log`；无供应商请求。截图 `workspace-evidence/final-offline-preview-confirmation.png`。
- 追加回归：workspace-ui、workspace-composer、workspace-media、workspace-asset-candidates 四个浏览器脚本均通过，未把单元测试替代UI点击验收。
- 新node测试均加入Harness。执行并发限制为2，避免8GB机器同时启动大量测试进程；没有删测试或弱化付费断言。现有浏览器脚本中的“媒体空间”定位改为“列表视图”aria标签；窗口化后点击离屏条目前先滚动并等待挂载。
- 专业画布新测试由“全局updateNode已拦截”的假定改为显式命令调用，原因是该全局接线经审查不安全；不把未启用接口写成已开放能力。

### 仍须完成，不能隐去

1. 专业画布 NodeInfoBar 本地参数、MG/自定义模型参数、参考连线及生成/恢复全链路的同源编辑和可读冲突提示。
2. 三层 Agent 范围的工具级强制权限：当前冻结目标、线程和提示词约束不等于硬权限隔离。
3. 原上传/未索引候选、完整音频归类与专业能力退役清单；文档外部编辑并发CAS和跨重启草稿恢复。
4. 三类真实旧项目打开、真实应用启动、任务恢复与持续流式性能验收；不违反禁止读取用户数据和真实供应商调用的边界。
5. 用户真实付费冒烟：取消无提交；单确认一次任务；不明状态不重提；切对象不串稿；重启后原taskId认领，回原候选；采用不改已有时间线。

以下为本轮之前的累计记录，数字与待办以本节最新核对为准。

---

## 历史记录

更新：2026-09-05。**阶段性报告，完整目标尚未完成。**

依据：桌面二次调研目录的 01、02、03 三份文档及本轮完整实施指令。原型闸门已解除，但仍按 A 类修复、阶段1、2、3、4 顺序推进。第一部分已完成；阶段1的生产 ProjectWorkspace 已组装内容列表、媒体预览/编辑、真实内嵌助手、草稿运行适配器和版本命令。加载完成且存在可浏览对象的工坊项目默认进入新媒体工作面；空项目保留旧录入流程，旧工坊由项目菜单“兼容工坊”进入，并可返回新工作台。阶段1补齐旧输出版本记录、渠道配置修复入口、按对象和参数隔离的手动费用查询，技术闸门已全绿，开始阶段2。容器与助手分别通过离线点击验收，不等同真实供应商或用户项目验收。

## 1. 安全与验证边界

- 未读取真实密钥；未发起供应商/外部 API 请求或真实付费任务。仅运行本地模拟测试和 localhost 界面夹具。
- 未修改 Rust、DSH 上游或版本、模型路由、billingSafety；未打包 DMG、提交/推送 Git、新建分支；未读写用户的真实项目作为测试样本。
- 新增确认信号贯穿内置协调器和 DSH 前端工具桥，未修改 DeepSeek 的降级目的模型。
- 磁盘 workshop.json 与 canvas.json 仍不是原子事务。选版/删除现在有单一业务入口及投影，其他双写路径留待阶段2，不冒称全部收敛。
- 阶段1新增 `strictPaidSafety` 可选参数，只有新工作台提交命令启用；旧图片入口的既有重试偏好未变。新模式对缺少提交回执的图片错误采取保守停止，不压缩重提或自动换渠道；工作台任务的恢复函数禁止另建替代任务。`billingSafety.ts` 本身未修改。
- `canvasGen` 的12个原生任务创建点增加可选 `workspaceBinding`，持久化提交对象、提示词、原图路径顺序、模型参数。这里只扩充原任务记录，不增加付费执行器、不改变任务所有权认领。
- `aigc/projectStore.writeProjectFile` 增加同文件串行写入队列和可选严格错误传播；workshop `commitNow({requireSuccess:true})` 供新工作台在付费边界前等待保存成功。旧调用仍保留既有错误处理方式；串行写入不是跨文件事务，也未用真实用户文件验证。
- `workspace/runtime.ts` 的生产适配器直接调用既有 canvasGen；任务投影订阅及 ProjectWorkspace 默认入口已在 App 接线。离线测试替换外部 store、磁盘和执行器，不挂载完整 App，不触发启动预连或供应商请求。
- `videoPrompt/prompt.ts` 仅追加可选 AbortSignal 参数；原调用的模型路由、提示词模板和续写策略不变。新工作台优化调用既有 quickChat/视频模板，不新造 LLM 路由，不在测试中调用真实模型。

## 2. 第一部分 A1–A6

| 项目 | 界面已可操作 | 数据与逻辑 | 尚未验证 |
| --- | --- | --- | --- |
| A1 确认队列 | FIFO 显示第 N/M 条、单项拒绝/确认、同轮可见生成草稿全部确认；隔离浏览器测试通过 | confirmationQueue、confirmBatch、toolConfirmStore；批量仅保留获准条目；abort/作用域取消逐一结算；旧弹窗点击不能批准下一项 | 真实供应商执行不测；旧页面各生成按钮的共同确认入口仍由新工作台接入阶段继续统一 |
| A2 参考语义 | 原有本次参考条和参考连线保留，新 Composer 已接列表与对象画布，无全局开关 | 删除无实际消费的全局 includeAsReference 写入/selector；旧字段仅兼容读取并幂等清除。无类型旧边保持参考，新类型仅 reference/workshop-reference 收集；其他关系不入 @ 编号 | 全项目专业节点、历史项目逐一 UI 打开留阶段4 |
| A3 三档偏好 | 原规格设置三档保留；默认仅付费生成前确认 | paid 来源为注册工具风险：safe 免费，ask/未知保守付费；不信任模型参数中的 paid。免费生成也进入偏好判断，内置/DSH 同规则 | 真实渠道账单未测；非生成工具仍保持原风险确认行为 |
| A4 选版/删除收敛 | 原画布/项目操作调用既有 unified store 入口 | projectCommands 一次计算业务数据和投影；保存前检查项目/修订；同媒体类型选版，不改其他媒体或历史候选；删除清理兼容字段、防止迁移复活 | 完整旧同步路径退出未做，详见双写审计；时间线真实 UI 回归留阶段3/4 |
| A5 快照常驻入口 | 项目顶栏“项目快照”按钮，列表和回到此刻；隔离界面通过 | 同时列出消息级和删除级快照，沿用 restoreProjectSnapshot/生成中确认；保留现有最多40个快照策略 | 浏览器夹具模拟持久化，没有用用户真实项目回滚 |
| A6 引用手势 | 工坊镜头描述框选浮出“添加到对话”；镜头对话按钮、资产预览可拖入助手。文字按钮隔离界面验证通过 | 只传项目/对象/来源身份，接收时解析当前 registry；跨项目、未知/已删对象拒收；引用保持原 @ 文本且默认只读 | 真实三视图完整拖拽尚未浏览器端到端测试；新工作台对象卡复用留阶段1/2 |

主要文件：

- A1：`src/lib/agent/confirmationQueue.ts`、`confirmBatch.ts`、`src/stores/toolConfirmStore.ts`、`src/components/ToolConfirmDialog.tsx`、`coordinator.ts`、`dsh/toolRpc.ts`、`src/hooks/useAgent.ts`。
- A2：`src/lib/canvas/referencePolicy.ts`、`collectRefsModel.ts`、`collectRefs.ts`、`src/lib/projectObjects/{migrate,selectors,collaboration,types}.ts`、`projectTools.ts`、`canvasStore.ts`。
- A3：`src/lib/projectObjects/generationDraft.ts`、`src/lib/canvas/generationDraftPreview.ts` 及 A1 确认入口。
- A4：`src/lib/projectObjects/projectCommands.ts`、`deletion.ts`、`selectors.ts`、`migrate.ts`、`src/lib/workshop/canvasSyncModel.ts`、`src/stores/unifiedProjectStore.ts`。
- A5：`src/components/projects/ProjectSnapshotPanel.tsx`、`ProjectTopBar.tsx`、`src/lib/projectObjects/snapshots.ts`。
- A6：`src/lib/projectObjects/referenceTransfer.ts`、`src/components/chat/ProjectReferenceSource.tsx`、`AgentDrawer.tsx`、`StepAssets.tsx`、`StepPrompts.tsx`。

## 3. 新工作台四阶段状态

| 阶段 | 界面已可操作 | 仅有数据接口/基础 | 尚未实现 |
| --- | --- | --- | --- |
| 1 项目壳与纵向切片 | ProjectWorkspace 默认媒体工作面，单层顶栏、列表媒体组、预览/提示词/候选采用、内嵌助手、快照与兼容入口；新增费用查询与缺配置修复入口，旧输出版本采用离线通过 | runtime 严格保存/确认队列/canvasGen；Agent 草稿工具；独立模型展示表和冻结参数；优化接原路由；旧版本幂等补齐。阶段1技术闸门已全绿 | 无估价接口渠道的实时费用不可提供；真实 LLM/付费/用户路径A验收未做 |
| 2 同对象画布 | 新 ReactFlow 对象卡、图片/视频组、同一预览器、列表画布切换、定位、移动、待整理区、纯画布；图片/视频独立草稿、未归类素材预览与归类；旧资产弹窗可见注册表候选并采用，离线点击通过 | 隐藏布局沿原保存/快照链；归类仅改 owner，不动参考/采用/历史快照。兼容 pullFromCanvas 已统一候选导入；单镜/增量改词、资产词/模型设置和 generateAsset 共用草稿命令。全量replace、场景多角度及旧镜头生成尚未收编 | 全量双写收编、专业工具定位入口、旧向画布推送同步退出；专业节点尚未投影归类后的 owner；阶段2尚未完成 |
| 3 全项目覆盖 | 文档面已有梗概/分场只读投影与规格/快照入口，剧本编辑退回兼容工坊；剪辑仍打开旧专业编辑器，导出复用既有 ExportDialog。导演卡的紧凑入口提前接到阶段1的视频 Composer，支持场景共享/本镜卡选择、候选采用和单稿启用 | 旧角色/场景/道具、专业剪辑能力仍在；内嵌助手隐藏时保留输入，消息排队捕获原对象；图片/视频未归类入口在阶段2实现 | 音频工作面/音频归属、完整文档编辑、时间线来源追踪、三层局部线程/硬范围隔离、剪辑工作面助手共用与队列跨卸载持久化；旧兼容视频入口不自动读取新场景共享卡 |
| 4 删除/性能/回归 | 仅旧入口有快照找回 | 删除纯命令与内存迁移测试 | 工作台删除菜单、100镜压力/流式响应、旧项目 UI 打开、任务恢复专项、退役清单 |

## 4. 自动检查

A 阶段与阶段1当前子项检查：

| 命令 | 结果 |
| --- | --- |
| `npx tsc --noEmit` | 通过 |
| `npm run test:harness` | 413/413，无跳过；阶段1新增61条，阶段2目前新增canvasLayout 5条、inbox 6条、legacyCanvasImport 6条、shotEdits 11条、assetEdits 4条、assetGeneration 6条、shotSettings 5条 |
| `npm run test:wan3` | 21/21 |
| `npm run test:midjourney` | 9/9 |
| `npm run test:dsh-runtime` | 5/5；TCP/进程均为本地测试夹具。握手超时用例有预期错误输出但测试通过 |
| `npm run build` | 通过；仍有旧主包体积超过500KB警告，未将其隐瞒为性能验收通过 |
| `node scripts/workspace-ui-smoke.mjs` | 通过；实际 React 组件、内存持久化替身、无应用 bootstrap。外部请求全部阻止，浏览器与本地服务器结束后关闭 |
| `node scripts/workspace-composer-smoke.mjs` | 通过；14张缩略图、7,560字正文、换序原路径、底部生成栏、预览 contain、1440/1280/1024/390 三栏降级与隐藏助手不占宽；助手为输入框替身 |
| `node scripts/workspace-media-smoke.mjs` | 通过；真实组件和项目命令的离线点击链：切镜头保存、异步优化 A/B 隔离与冲突拒绝、取消零提交、仅一次 mock 执行、结果回原对象候选、手动采用、重复版本断言、参考选择与批量对话引用 |
| `node scripts/workspace-assistant-smoke.mjs` | 通过；实际 AgentDrawer/WorkshopChatPanel，模型/会话/部分子控件替身。排队消息捕获 A/B 目标、失败保留、隐藏后新回复不撑开、跨工作面未发送文本保留、四档宽度发送键可见。未覆盖真实模型执行 |
| `node scripts/workspace-asset-candidates-smoke.mjs` | 通过；实际旧 AssetCandidatesModal + 统一选版纯命令，模拟 store/生成/文件系统端口。注册表独有候选可见、采用保留旧版本、再生成失败可读；无真实供应商请求 |
| `node scripts/workspace-project-smoke.mjs` | 通过；实际 ProjectWorkspace 容器，store/执行器/助手/规格快照等外部端口替身。选镜头、改词、A/B 异步返回、文档往返、取消零提交、单次 mock 生成、候选采用、添加到对话、快照入口与四档宽度检查。追加场景约束卡创建/改词/场景图参考/确认前零提交/候选采用/本镜启用与编号，切镜头不自动启用 |

阶段2此切片新增 `workspace/canvasLayout.test.ts` 5条，已挂 Harness；现有测试没有改期望。容器脚本增加实际 ReactFlow 拖动、新角色入待整理不抢焦点、列表/画布共享草稿与媒体、画布生成确认前无执行、确认后一个 mock 调用、切回列表回流候选且不改采用和参考集合。助手、持久化、外部执行器仍为替身，不能当成真实账号生成证据。

阶段2此次追加 `workspace/inbox.test.ts` 6条与 `legacyCanvasImport.test.ts` 6条，均挂 Harness。初次新增未改旧测试；后续兼容读投影调整了一项 legacyCanvasImport 断言，理由见既有测试修改清单第5项。前者验证独立空白目标、取消零调用、相同 canvasGen 命令/严格模式、归类不改引用或采用、锁定/过期拒绝、恢复重放不反转归属、老无归属素材不伪造参数。后者验证兼容拉取只进候选、稳定镜头 ID、正文/参考顺序、现有草稿冲突保护、不由音频连线自动换音色，以及真实 pullFromCanvas 函数只调用一个发布命令的离线端口测试。

随后追加单镜编辑收敛：`shotEdits.test.ts` 前7条覆盖新/旧提示词编辑同源、正文与参考同时投影、图片/视频参考隔离、模板与全局继承、旧Agent版本冲突与撤销、迁移修复投影且不改历史、锁定时保留任务状态回执。`updateShot`、`applyWorkshopShotPatch` 和撤销已接同一纯命令。Workspace 保存只投影对应的旧提示词和本次参考，不改剧情/对白/关系或产物。生产容器离线脚本验证旧编辑命令返回后新输入框立即显示同一正文。

继续追加增量批量入口：`setShots(..., 'merge')` 接 `mergeWorkspaceShots`，复用单镜草稿命令；稳定ID优先、无ID兼容镜号、支持互换镜号，最终镜号/对象重复或锁定先整批拒绝。新增4条测试验证同稿修订、参考不重置、历史参数保留、重编号/新对象、拒绝不落半批及旧无ID数据。当前11条均挂Harness。`replace` 全量重拆仍需与删除命令/快照一并收编；资产词与资产模型/比例/分辨率、镜头视频模型/比例/时长在后续小步收编；原参考编辑器和专业节点编辑仍待办。未声称所有批量入口已完成，也未对真实用户项目执行重拆。

容器脚本已追加“新建素材 → 图片素材 → 改词 → 确认前 mock 调用数不变 → 确认后恰好一次 → 未归类 → 归类先取消 → 再归镜头02候选”。断言原镜头采用版/参考不变、同一 media/version ID 与冻结快照保留；390px 归类按钮不越界。画布中新建视频草稿也不继承先前镜头引用，不触发提交。

当前切片最终验证：tsc、Harness 413/413、Wan3 21/21、Midjourney 9/9、DSH 5/5、build 与六条浏览器脚本全部通过。对象画布共享预览另有1440/1280/1024/390截图，逐档检查生成按钮在视口内；另测390px归类操作按钮边界。参数互通在实际 ProjectWorkspace 容器离线通过，截图见下。这是布局与功能验收，不是100镜性能验收；所有临时浏览器/本地服务器已关闭。构建仍有旧大包、动态/静态导入混用和 Browserslist 数据过期警告，未通过联网更新依赖消除它们。

新增 node:test 文件已手动加入 test:harness：confirmationQueue、collectRefsModel、generationConfirmation、projectCommands、snapshotList、referenceTransfer。原 referencePolicy 专项也加入 Harness。

阶段1追加 `src/lib/workspace/workspaceDrafts.test.ts`（8条）、`generationCommand.test.ts`（12条）、`src/lib/imageGen/attemptPolicy.test.ts`（3条），均挂入同一脚本。分别覆盖草稿隔离/参考重编号/历史参数与候选、6组实际命令偏好矩阵/取消不提交/保存失败不提交/项目切换/不明状态不重试、严格图片策略及12处原生任务元数据接线静态契约。Mock 不等同供应商端真实执行证据。

本次追加并挂入 Harness：`aigc/projectWriteQueue.test.ts`（2条）、`workspace/taskProjection.test.ts`（3条）、`runtime.test.ts`（1条）、`mediaView.test.ts`（1条）、`contentModel.test.ts`（3条）、`versionProjection.test.ts`（2条）。分别验证同文件写盘顺序/失败传播，任务跨项目投影与孤立状态，实际适配器的 mock 集成，历史参数不取当前草稿，场景往返不改剧情顺序/稳定选择，镜头和角色采用生成版后反复迁移不重复建媒体且保留任务来源与历史参数。

界面测试发现并修复 `migrate.ts` 的额外缺陷：采用生成结果后，旧路径投影会被识别成新上传素材/新镜头媒体，导致相同原图出现两次甚至两个 v1。现沿原生成版本和媒体 ID 重建投影，保留 generationSnapshot、generationTaskId。未删除既有测试或改弱候选/参考断言；版本迁移新用例单独追加。

最新追加并挂入 Harness：`workspace/agentIntegration.test.ts`（3条）、`engineCatalog.test.ts`（2条）、`optimize.test.ts`（1条）、`entryPolicy.test.ts`（1条）。覆盖共享工具注册及修订/锁定/中止保护、独立路由与别名、显示默认参数进入提交快照、优化信号和引用校验、默认入口项目身份匹配。工具注册用例包含源码接线断言，不能视为已完成真实 DSH RPC 调用验证。

模型展示表 `workspace/engineCatalog.ts` 仅描述现有 canvasGen 路由，不添加 RH GPT/MJ 通道。`materializeWorkspaceDefaults` 将缺省参数补进本次草稿/确认/历史，不覆盖显式旧参数。`useWorkspaceServices` 只向组件传配置是否存在，不传密钥；缺配置模型置灰并可原位打开设置。CLI 登录/余额未知不伪装为已验证可用。手动费用查询复用原 RH previewPrice，明确标注渠道，不代表最终智能路由账单；无估价接口显示“费用以渠道结算为准”，不当成免费，确认仍按注册工具风险执行。

阶段1最终补测：`projectObjects/legacyVersions.test.ts` 3条（旧图片/视频/音频输出补稳定版本，重复迁移不变，采用/删除不复活），`workspace/services.test.ts` 6条（能力配置、估价参数、缺接口/错误不免费、报价隔离、实际价格解析器离线传输）。已挂 Harness。旧历史缺失的模型参数不编造。`pricePreview.ts` 现拒绝缺价格的 HTTP 200，避免原默认零元造成误导；没有修改既有测试预期。容器点击追加报价、配置入口、旧视频/图片版本采用，全部模拟端口。

导演约束卡追加 `workspace/constraints.test.ts`（10条，已挂 Harness）：场景/本镜元数据与幂等迁移、只传场景像素、追加引用与编号、旧首位卡重排、修订冲突、同一确认链/候选版/采用后旧草稿提醒、取消或错误素材不提交、场景/卡删除清理、锁定、旧卡提示词读取及 Agent 独立编辑范围。均为内存/mock，未生成任何真实约束卡图片。

落点：`WorkspaceConstraintSection.tsx` 默认 `<details>` 收起，仅在视频 Composer 内展开；`constraints.ts` 把本镜启用写入该视频草稿，卡的生成提示词另存 `director-constraint:ID::image`。`generationCommand.ts` 在确认前及付费边界前校验参考仅属当前场景；候选继续由 canvasGen 任务绑定回流，采用仍走统一选版入口。`WsScene.directorConstraintCard` 是可选的兼容新增字段，旧镜头卡读取保持。删除场景/卡同时清理所属卡草稿及其他草稿引用，正文遗留引用变成显式待修标记，阻止静默错号；已提交快照不改。

### 既有测试修改理由

1. `src/lib/agent/dsh/bridge.test.ts` 的运行中取消测试：先等待 mock 工具体真正开始再取消，避免将“执行前取消”错误地断言成“工具体看到取消”。新增执行前 guard 的正确结果是工具体完全不执行。
2. `src/lib/projectObjects/migrate.test.ts`：删除死 `generationReferenceMedia` selector/全局标记断言，改测旧标记幂等清除、对象 ID 和镜头引用不漂移。原因是 A2 明确要求移除两套参考语义，原标记本来没有生成路径消费；不是删除付费或引用保护测试。
3. 同文件追加旧字段迁移测试；`projectCommands.test.ts` 新增删除/换版实际投影用例。没有为了使测试通过删掉旧任务恢复和计费断言。
4. `workspace/agentIntegration.test.ts` 的“整个 shots 数组完全不变”断言改为：除当前 imagePrompt 及只读参考投影外所有字段不变、另一镜头完全不变，并断言旧字段与共享草稿一致。原因：阶段2明确要求旧读路径改为投影，原断言会固化“旧提示词仍保持旧值”的双写缺陷；对白/关系/描述保护没有放宽。
5. `workspace/legacyCanvasImport.test.ts` 同理把整个 shot 不变改为：仅 videoPrompt 和只读参考投影同步，其他字段逐一深比较不变。导入后的正文/参考必须与共享草稿一致；候选不自动采用、历史参数不编造的断言未改。
6. `workspace/runtime.test.ts` 在原测试内追加资产取消零调用、确认后同一 canvasGen 端口、对象绑定、候选不自动采用与项目切换断言；mock 输出扩展名由一律 mp4 改为按输出类型 png/mp4，匹配被测图片入口。没有移除或放宽原镜头断言。

## 5. 界面证据与复现

资产编辑追加（阶段2）：`assetDraftModel.ts`/`assetEdits.ts` 收编 setAssetPrompt、setAssetEngine、setAssetResolution、setAssetAspectRatio。初始化按选中模型读取普通/MJ词，并读取资产自身比例/分辨率；当前草稿保存投影对应词槽和设置，另一个词槽保留。首次打开即切模型与后续切换均可读取目标词槽；同时显式改写的正文不被切换逻辑覆盖。角色外观/性格、场景描述、采用图、历史提交快照均不变，锁定或已删对象拒绝保存。新增 assetEdits.test.ts 4条已挂Harness，原测试未修改。实际容器离线脚本新增“打开司机 → 首次切MJ/切回 → 普通改词 → 切MJ → 兼容命令改MJ → 切回普通词”，断言四次mock生成总数不增加。测试端口不运行旧工坊完整页面。

资产生成追加（阶段2）：旧 generateAsset 只委派 runtime.generateWorkspaceAsset，由 assetGeneration.ts 准备当前对象可见草稿，走原确认队列/严格保存/canvasGen/任务绑定。取消不执行，结果只入候选，提交不明不重提。旧 AssetCandidatesModal 与 StepAssets 候选计数读取 assetCandidates.ts 的同一注册表投影；已索引候选的采用调用统一选版命令。旧上传/场景多角度尚未索引的候选仍向后兼容显示和采用，不冒称这些写入口已经收编。新增 assetGeneration.test.ts 6条已挂 Harness，并有实际运行适配器和旧弹窗离线测试。

明确取舍：提交时不再暗中拼风格长词、强制把角色描述替换成三视图词，也不在旧 generateAsset 内自动生成/挂接 MJ 风格参考图。以用户可见的当前草稿正文、参数与显式参考为准，空提示词先报错。旧风格选择/改写能力未删除，但旧提交时隐藏风格补写和自动参考物化需要转入可审阅草稿，后续仍须完善，不能声称完全保留了该自动行为。旧生成文件复制/首次自动采用移除；canvasGen 已持久化的结果由版本库共享原路径。

镜头参数追加（阶段2）：shotDraftModel.ts 统一视频模型别名和继承解析；保存草稿只在参数实际改变时投影单镜覆盖，不因单纯改词把所有镜头锁死成单独模型。editWorkspaceShot、mergeWorkspaceShots、setVideoModel/setVideoRatio 共用草稿写入，模型切换复用能力表校准；删掉的超额参考在正文标记待修，不静默错号。全局只更新仍继承字段，遇继承中的锁定镜头整批拒绝；旧设置面的错误提示已接线，未单独进行该页面的点击验证。shotSettings.test.ts 5条已挂 Harness。旧入口尚无完整参数调整明细提示，新 Composer 自身切模型有调整说明。

剩余边界：全量replace、原参考编辑器、旧镜头/场景多角度生成、专业节点写入仍待阶段2。没有对真实项目提交生成或修改用户媒体。

以下均为离线夹具；包含生产组件/容器，但不是用户真实项目与供应商执行截图：

- [旧资产候选弹窗](workspace-evidence/phase2-legacy-asset-candidates.png)：旧弹窗直接显示注册表新候选 → 点候选 → 设为最终图 → 旧版保留 → 再生成返回模拟错误并显示；本地测试照片不代表真实生成效果。
- [共享视频设置](workspace-evidence/phase2-shared-video-settings.png)：新工作台打开01视频提示词 → 兼容命令设置2.5/15秒/9:16 → 控件即时显示 → 新编辑器改H3/7秒 → 旧镜头字段和左侧时长同步；mock执行数没有增加。右侧白色输入框是助手替身。
- [确认队列](workspace-evidence/a1-confirmation-queue.png)：并发请求3条 → 确认1 → 拒绝2 → 确认3，断言执行许可 `[true,false,true]`；另一次全部确认断言顺序1、2、3。
- [390px确认按钮边界](workspace-evidence/a1-confirmation-390.png)：同时检查1440/1280/1024/390宽度操作按钮均在视口内。
- [快照列表](workspace-evidence/a5-snapshots.png)：项目快照 → 同时显示删除与消息快照 → 回到此刻，调用 mock restore 的目标为当前删除快照。
- [框选引用](workspace-evidence/a6-selection.png)：框选含 @图片一 的文字 → 添加到对话 → 断言原文保持、kind=text-selection、scope=read。
- 拖入纯数据证据：referenceTransfer.test.ts 校验同项目身份解析、过期标签不使用、跨项目/删除对象拒绝。尚不能等同真实拖拽链路已验收。
- [提示词编辑器 1440](workspace-evidence/phase1-composer-1440.png)、[1280](workspace-evidence/phase1-composer-1280.png)、[1024](workspace-evidence/phase1-composer-1024.png)、[390](workspace-evidence/phase1-composer-390.png)：仅为真实 Composer 组件的离线夹具。后移第1张素材 → 正文 @图片一/二对应换序 → 优化选新版 → 注入7,560字正文 → 生成回调收到完整正文和14条原图路径。示例图片复用仓库本地素材，不代表实际镜头或新生成结果。
- [真实比例预览](workspace-evidence/phase1-inspector-1440.png)、[窄屏预览与编辑](workspace-evidence/phase1-inspector-edit-390.png)、[三栏壳](workspace-evidence/phase1-shell-layout-1440.png)：壳层夹具中的目录与助手是简单替身，不能证明真实助手已经接入。
- [镜头闭环点击验收](workspace-evidence/phase1-media-flow-1440.png)：内容列表选01 → 提示词 → 保存 → 选02并保存 → 回01 → 优化后切02，结果只写01 → 人工改01后旧优化拒绝覆盖 → 生成并取消，mock 执行数0 → 再次生成确认，执行数1 → 切02后完成原任务 → 01候选版本出现 → 采用v2。使用真实项目纯命令和组件；磁盘、供应商、模型回复和助手为模拟，图像复用仓库素材。不能作为用户项目/付费实测证据。
- [生产容器1440](workspace-evidence/phase1-project-container-1440.png)、[390](workspace-evidence/phase1-project-container-390.png)：真实 ProjectWorkspace 的同一链路，并验证文档往返、默认 GPT 可选、候选采用后同一版本出现在目录媒体组和预览器；所有照片复用仓库现有素材测试 contain，照片不代表脚本内容，右栏是明确的输入框替身。
- [真实内嵌助手1440](workspace-evidence/phase1-assistant-1440.png)、[390](workspace-evidence/phase1-assistant-390.png)：实际抽屉组件的独立测试，项目中栏为占位。隐藏时注入新回复不打开；队列 A/B 分别持有发出时对象；未发送文字跨文档面保留。与上面的容器夹具是互补证据，不声称一次完整真实 App 冒烟。
- [空间与调度展开](workspace-evidence/phase1-director-constraint-expanded.png)：选镜头01 → 视频 → 提示词 → 空间与调度 → 共享场景约束 → 建立 → 改词 → 生成约束卡 → 确认 → mock回流 → 采用 → 本次视频使用。断言参考只有 `/road.jpg`、生成之前无新增执行、正文 `@导演约束卡（对应 @图片三）` 与参考条一致，镜头02不自动启用。图片仅为仓库照片替身，不是生成的灰模卡，不能作为生成质量证据。
- [画布与共享预览器](workspace-evidence/phase2-canvas-shared-inspector.png)、[纯对象画布](workspace-evidence/phase2-pure-object-canvas.png)：列表选02 → 画布 → 点卡内图片v1 → 原预览器打开 → 关闭预览后拖动02 → 新增角色，02位置/选中不变 → 回列表写草稿 → 回画布看同一稿 → 模拟生成确认 → 回列表得到候选，旧采用图保持。图像复用本地测试照片；右侧白色输入框是助手替身，不是生产助手样式。
- [未归类结果](workspace-evidence/phase2-unclassified-generation.png)、[归类弹窗](workspace-evidence/phase2-classify-material.png)、[归类后候选](workspace-evidence/phase2-classified-candidate.png)：局部工具栏“新建素材” → 图片素材 → 提示词 → mock确认/完成 → 预览器文件夹图标“归类素材” → 镜头02 → 归入候选组。引用、采用路径与提交历史均未自动更改；图片是本地复用照片，不是该提示词真实生成效果。

## 6. 02号文第18节逐条自评

| # | 状态与证据 |
| --- | --- |
| 1 单层项目顶栏 | 部分：新媒体工作面默认入口已接 App，48px单层顶栏且不显示旧顶栏/全局侧栏；容器截图。剪辑/专业画布仍旧入口 |
| 2 两次点击到完整提示词 | 已验证媒体工作面范围：真实容器点击“镜头→提示词”即全文；未用用户真实项目启动 App 验证 |
| 3 同一预览器/版本 | 部分：列表与新对象画布使用同一挂载的 MediaInspector，点击验收通过；剪辑与旧专业节点未接 |
| 4 三栏响应式 | 部分：容器/助手及对象画布共享预览通过1440/1280/1024/390边界测试，窄屏覆盖目录；截图 phase2-canvas-inspector-各宽度.png；实际用户 App/系统缩放未测 |
| 5 隐藏助手不占宽/不强开 | 已验证内嵌范围：实际抽屉隐藏后注入新消息仍不打开，输入跨工作面保持；真实模型输出来流未测 |
| 6 对象/媒体/采用动作区分 | 已验证媒体工作面范围：列表/画布标题选对象、内部媒体选版本打开预览、采用独立动作；mock生成不自动采用 |
| 7 改提示词不改事实对白 | 部分：新工具校验草稿修订/锁定；旧 updateShot、Agent单镜改词/撤销与 setShots merge 已写同一草稿并投影对应正文/参考，shotEdits 11条及容器互通点击验证。资产词和资产模型设置另有4条及容器切换测试；replace 与专业节点尚未收编；提示词约束不等于完整硬权限隔离 |
| 8 异步 A/B 草稿隔离 | 已验证媒体工作面范围：真实组件及容器离线点击保留A/B草稿、迟到结果只写A、修订冲突拒绝；生产回调同样捕获草稿与signal |
| 9 结果候选、不自动参考/时间线 | 部分：runtime/taskProjection 与组件 mock 完成回原对象候选，空白生成进未归类；归类不采用、不改引用/原提交快照；兼容拉取也不再自动采用。其余旧生成/恢复路径还待收编，未真实提交 |
| 10 参考/编号/提交/历史参数 | 部分：collectRefsModel边序、Workspace参考换序、原图路径/默认参数进入提交与历史快照有测试；新对象画布复用同一草稿，移动不改引用；旧专业节点同步未收编 |
| 11 空间与调度紧凑态 | 已验证新媒体工作面范围：视频提示词下默认单行收起，展开可选共享场景/本镜卡、改词、候选生成采用和单稿引用；离线点击及 constraints.test.ts。卡升级只提醒旧稿，不自动重生视频；旧兼容入口的场景共享卡消费留阶段2 |
| 12 故事板仅历史 | 部分：迁移/删除复活回归通过；旧项目完整 UI 验证未做 |
| 13 无传入/回传/拉取 | 未通过：新工作台无同步按钮；兼容拉取内部已统一候选导入，但旧向画布推送与专业工具路径尚未退出 |
| 14 队列/输入/会话跨工作面 | 部分：实际内嵌助手队列记录projectId/context/files，排队后切镜头仍操作原目标，媒体/文档切换不卸载助手；跨剪辑/专业画布卸载后的消息队列未持久化 |
| 15 提交不明不重提/取消不提交 | 已验证的范围：计费全绿；实际 runtime mock 中严格保存失败/取消/项目切换均不提交；容器取消执行数0、确认只执行1次；strict图片失败不重试。生产按钮已接原canvasGen，真实付费未测 |
| 16 100镜与流式性能 | 未通过：尚未压力测试，不用构建通过代替性能证明 |
| 17 专业能力可找而不负担默认页 | 部分：媒体工作面已无六步导航，项目菜单可进兼容工坊；空项目/剪辑/专业画布仍保留旧结构，完整退役清单未落实 |
| 18 用户认可三条路径 | 未通过：路径A的媒体修改/生成/选版有离线点击证据，但未真实模型/用户认可；路径B/C未完整实现 |

## 7. 后续与真实付费冒烟

阶段1技术闸门已通过。旧数据中有明确归属的 imagePath/videoPath/音频输出已补可采用版本；未知旧节点与真实旧项目打开仍留阶段2/4。下一步开展阶段2画布投影和旧同步退出、阶段3全项目覆盖、阶段4性能与老项目回归。无估价接口的渠道仍不能提供实时价格，真实付费验收由用户执行。当前文档面是只读梗概/分场与结构化规格编辑入口，不能冒充完整原剧本编辑器。导演约束卡已接新工作面，不等于旧入口与新草稿的全部写入已收敛；上传/素材库选卡入口仍在兼容工坊，新区域目前提供场景图生成与候选采用。

阶段2当前顺序：对象画布与图片/视频未归类链已接生产组件；兼容从画布导入已统一为候选/冲突安全草稿。接下来先收编旧向画布推送、workshopStore/旧 Agent 提示词写入、专业节点生成与恢复的剩余双写路径，再进入阶段3。不能把本次全绿解释为阶段2已经验收完成。

此次兼容取舍：拉取不再重复拷贝画布已持久化文件，不由音频参考连线自动更换角色音色；稳定对象绑定的输出只进候选，采用另行确认。已有工作台草稿冲突时仅显示数量并保留原稿，目前还没有逐字段冲突选择 UI。专业节点对应已归类素材的 owner 元数据尚未统一投影，音频归属和完整文档/剪辑面仍未完成。上述缺口都留在后续范围，不作为已完成交付。

阶段1保守取舍：部分同步图片渠道无法精确区分本地预处理错误与上游提交错误，新工作台 strict 模式会停止并提示先核对，不自动尝试下一渠道。后续应补齐可信的提交阶段标记以区分“确定未提交”和“结果未知”，不能用错误文字猜测后自动重试。当前策略不影响未传 strict 参数的旧入口。

`WorkspaceGenerationPort`、优化回调、内嵌助手和版本命令已由 ProjectWorkspace 接入，App 仅在身份完全匹配的已加载项目上替换默认工坊页面。优化模型请求和实际生成都未发起。局部修改的硬范围隔离、旧工具对新草稿的统一写入口、真实 DSH 工具调用冒烟尚缺，不能只凭工具注册或系统提示词就声称三层 Agent 范围完成。

真实付费冒烟留给用户：确认前取消无任务；确认后只产生一次供应商任务；明确失败与状态待核实区分；生成结果回正确对象候选组；切镜头不改目标；采用版不改已有时间线；应用重启后任务认领和结果回流正确。本报告不声称这些已实测。
