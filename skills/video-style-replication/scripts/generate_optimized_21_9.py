#!/usr/bin/env python3
"""
基于豆包分析的精准优化方案
画幅：21:9（3168x1344, 2K分辨率）
油画感：仅中远景35%，近景写实70%
"""
import os
import base64
import requests
from pathlib import Path
import sys

API_KEY = os.environ.get("DMXAPI_KEY")
BASE_URL = os.environ.get("DMXAPI_BASE_URL", "https://www.dmxapi.cn") + "/v1"

def generate_optimized_version(ref_image_path):
    """生成精准优化版本（21:9画幅）"""

    # 读取参考图
    ref_image_path = Path(ref_image_path)
    with open(ref_image_path, "rb") as f:
        ref_image_base64 = base64.b64encode(f.read()).decode()

    prompt = """
【核心指令】
参考附件人物形象，完全复刻面部特征，生成一张震撼的单幅画面：女主角在雨崩看到悬浮的巨型转经筒矩阵。

【重要：基于豆包分析的精准优化方案】

**画幅比例**：21:9超宽幅（3168x1344, 2K分辨率）

**未来废土风光影系统（4层光影）**：
- 主光层：侧逆日光（5600K暖黄）
- 辅光层：植被反射冷绿环境光（4200K）
- 体积光层：丁达尔光束（柔和散射光）
- 补光层：暗部弱补光
- 明暗反差：7-8档高反差，但阴影被大气柔化

**色彩系统**：
- 色温：4800K（暗部+15绿色偏移，亮部保留暖黄）
- 饱和度：低饱和（-18）
- 天空：浓郁钴蓝色
- 植被：低饱和灰绿色
- 对比度：全局+22，但整体通透朦胧

**质感系统（分层处理）**：

**近景（70%写实度，清晰边缘）**：
- 人物：清晰边缘，真实肌理
- 近景经幡：清晰边缘，真实布料质感
- 近景野花：清晰边缘，真实植物细节

**中远景（35%油画笔触感，边缘柔化）**：
- 转经筒矩阵：35%笔触感，哑光质感，边缘柔化
- 能量光束：35%笔触感，柔和发光，边缘柔化
- 梅里雪山：35%笔触感，边缘柔化
- 天空：35%笔触感，色彩过渡柔和（油画叠色效果）

**整体质感**：
- 哑光油画磨砂质感
- 无强烈高光反射
- 大气带有朦胧雾感颗粒
- 手绘肌理感（35%）

【画面内容】

**主体巨物：几何符号化转经筒矩阵（占比60%）**
- 位置：梅里雪山上空（海拔6000-8000米）
- 占比：占画面60%（向画面上部、左右延伸，撑满）
- 数量：数百个巨型转经筒（直径50-100米）
- 排列：螺旋上升排列，形成通天塔结构（总高度10公里）

**几何抽象化设计**：
- 形态：简化的完美圆柱体
- 表面：无真实纹理，只有发光的几何符号（圆形/方形）
- 材质：纯色块（金色），光滑无磨损，哑光质感
- 细节：发光的几何符号代替六字真言
- 顶部和底部：圆形光环
- 质感：35%油画笔触感，边缘柔化

**能量光束：生物化实体（占比约20%）**
- 设计：金属化的藤蔓/树根
- 形态：巨大的藤蔓（直径5-10米）
- 表面：有机纹理（树皮/鳞片），金属化
- 内部：能量液体（流动的蓝色/金色液体）
- 节点：能量果实（发光）
- 质感：35%油画笔触感，柔和发光，边缘柔化
- 连接：转经筒之间的生物化连接结构

**环境场景（4层景深）**：
- 第一层近景：雨崩高山草甸（紫色野花、绿草），清晰写实
- 第二层中景：彩色经幡阵列（飘动），清晰写实
- 第三层中远景：转经筒矩阵 + 能量光束，35%油画感
- 第四层远景：梅里雪山主峰 + 天空，35%油画感

**人物（占比2%）**：
- 位置：草甸中心，背对镜头，仰视巨物
- 景别：全景（人物占比2%，极小，强化巨物感）
- 姿态：站立，微微仰头，震撼凝视
- 服装：完全复刻参考图人物
  - 白色做旧半护甲（短款露腰）
  - 白色做旧机甲百褶裙
  - 背部交叉：藏刀+登山杖
  - 黑色湿发
- 面部：完全复刻参考图人物的面部特征
- 比例：真人比例，无动漫夸张
- 质感：70%写实度，清晰边缘

**构图（21:9超宽幅）**：
- 视角：横向广角（20-24mm），低仰视
- 画幅：21:9横向（3168x1344）
- 景深：4层（近景 → 中景 → 中远景 → 远景）
- 人物位置：画面中下1/3区域，居中或偏左
- 巨物位置：画面中上部，占60%，向左右延伸
- 横向延展：强调开阔感，预留横向延伸的空白空间
- 大气雾效：拉开近中远景的层次，增加朦胧感

**氛围**：
- 奇幻史诗感 + 失落文明探索感
- 静谧又壮阔
- 神圣的荒野探险氛围
- 通透朦胧，不阴郁

**光影细节**：
- 柔和散射光（代替硬直射光）
- 丁达尔光效（穿过转经筒间隙）
- 阴影被大气柔化，无强烈明暗对比
- 整体光照通透朦胧

【禁止】
- 图片中出现任何文字、字幕、标签
- 强行修改透视比例（保持自然透视）
- 拉高饱和度、对比度
- 过度磨皮/锐化
- 美化式光影优化
- 动漫化、二次元渲染
- 近景元素使用油画感（近景必须写实）
- 强烈的金属反光（使用哑光质感）

【总结】
- 画幅：21:9（3168x1344, 2K）
- 风格：未来废土风 + 哑光油画磨砂
- 转经筒：几何符号化（抽象），60%占比，35%油画感
- 能量光束：生物化实体（金属藤蔓+能量液体），35%油画感
- 人物：复刻参考图，2%占比，70%写实度
- 场景：雨崩草甸+梅里雪山+经幡，4层景深
- 构图：21:9横向广角，低仰视，巨物压迫感
- 氛围：奇幻史诗感 + 哑光油画磨砂 + 朦胧雾感
- 光影：柔和散射光 + 丁达尔光效
- 色彩：钴蓝天空 + 低饱和灰绿 + 冷调为主
"""

    output_path = Path.home() / "Desktop" / "视频复刻" / "精准优化版_21_9.jpg"
    output_path.parent.mkdir(parents=True, exist_ok=True)

    print("\n" + "="*80)
    print("【精准优化版（基于豆包分析）】")
    print("画幅：21:9（3168x1344, 2K分辨率）")
    print("="*80 + "\n")

    print("🎨 正在生成...")
    print("  ✓ 画幅：21:9超宽幅（3168x1344）")
    print("  ✓ 转经筒：几何符号化，60%占比，35%油画感")
    print("  ✓ 能量光束：生物化实体，35%油画感")
    print("  ✓ 人物：2%占比，70%写实度")
    print("  ✓ 质感：近景写实 + 中远景油画")
    print("  ✓ 光影：柔和散射光 + 丁达尔光效")
    print("  ✓ 色彩：钴蓝天空 + 低饱和灰绿")

    # 使用 /v1/images/generations 接口
    response = requests.post(
        f"{BASE_URL}/images/generations",
        headers={
            "Authorization": f"Bearer {API_KEY}",
            "Content-Type": "application/json"
        },
        json={
            "model": "gpt-image-2",
            "prompt": prompt,
            "n": 1,
            "size": "3168x1344"
        },
        timeout=600
    )

    result = response.json()

    if "data" in result and len(result["data"]) > 0:
        image_data = result["data"][0]

        if "b64_json" in image_data:
            img_data = base64.b64decode(image_data["b64_json"])
            with open(output_path, "wb") as f:
                f.write(img_data)
            print(f"\n✅ 精准优化版已保存: {output_path}")
            return output_path
        elif "url" in image_data:
            img_response = requests.get(image_data["url"], timeout=30)
            with open(output_path, "wb") as f:
                f.write(img_response.content)
            print(f"\n✅ 精准优化版已保存: {output_path}")
            return output_path

    print(f"❌ 生成失败: {result}")
    return None


def main():
    # Usage: python3 generate_optimized_21_9.py <参考图路径>
    if len(sys.argv) != 2:
        print("Usage: python3 generate_optimized_21_9.py <参考图路径>")
        sys.exit(1)
    result = generate_optimized_version(sys.argv[1])
    if result:
        print("\n" + "="*80)
        print("【生成完成】")
        print("="*80)
        print("特点：")
        print("  1. 画幅：21:9超宽幅（3168x1344）")
        print("  2. 巨物占比：60%，压迫感强")
        print("  3. 人物占比：2%，极小")
        print("  4. 油画感：仅中远景35%，近景写实70%")
        print("  5. 能量光束：生物化实体")
        print("  6. 氛围：奇幻史诗感 + 哑光油画磨砂")


if __name__ == "__main__":
    main()
